# Experiment Cases

Cases are grouped by dataset and then by architecture. The active MNIST PCA-16 case is `mnist_pca16/local_routing_5x16_reference_93`, configured as the z-score PCA phase-encoding default.
