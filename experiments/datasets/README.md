# Datasets

Each dataset gets its own folder with raw downloads, processed arrays, and preparation notes. Generated data is ignored by `experiments/.gitignore`; keep only configs and documentation under version control.
